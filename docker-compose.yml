services:
  app:
    build: .
    ports:
      - "8000:8000"
    environment:
      - REDIS_URL=redis://redis:6379
      - DATABASE_URL=sqlite:///./fraud_logs.db
    volumes:
      - .:/app
    depends_on:
      - redis

  redis:
    image: redis:alpine